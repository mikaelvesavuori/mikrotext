{
  "name": "mikrotext",
  "description": "Short-lived encrypted text rooms with no accounts and no server-readable messages.",
  "version": "1.0.2",
  "author": "Mikael Vesavuori",
  "license": "MIT",
  "keywords": [
    "chat",
    "encrypted-chat",
    "ephemeral-chat",
    "end-to-end-encryption",
    "mikrosuite"
  ],
  "main": "lib/mikrotext.mjs",
  "module": "lib/mikrotext.mjs",
  "sideEffects": [
    "./app/scripts/*.mjs"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mikaelvesavuori/mikrotext"
  },
  "bugs": {
    "url": "https://github.com/mikaelvesavuori/mikrotext/issues"
  },
  "homepage": "https://github.com/mikaelvesavuori/mikrotext",
  "files": [
    "/lib",
    "!/lib/**/*.map",
    "!/tests"
  ],
  "scripts": {
    "start": "npm run dev",
    "assets": "node ../mikrosuite-site/brand/generate-product-icons.mjs --product mikrotext",
    "dev": "npm run build:web && concurrently \"npm run dev:web:serve\" \"npm run dev:api\"",
    "dev:web": "npm run build:web && npm run dev:web:serve",
    "dev:web:serve": "http-server dist -a ${HOST:-127.0.0.1} -p ${WEB_PORT:-8000} -c-1 -P http://127.0.0.1:${WEB_PORT:-8000}?",
    "dev:api": "tsx src/index.ts",
    "docs": "npm run docs:install && npm --prefix docs run dev",
    "docs:build": "npm run docs:install && npm --prefix docs run build",
    "docs:preview": "npm run docs:install && npm --prefix docs run preview",
    "test": "vitest run --config vitest.config.mts --coverage",
    "test:watch": "vitest --config vitest.config.mts",
    "typecheck": "tsc --noEmit",
    "build": "npm run clean && npm run build:web && npm run build:api",
    "build:api": "node esbuild.config.mjs",
    "build:web": "node build.mjs",
    "clean": "node -e \"const fs=require('node:fs');for (const path of ['lib','dist']) fs.rmSync(path,{recursive:true,force:true});fs.mkdirSync('lib',{recursive:true})\"",
    "lint": "biome lint app src tests scripts build.mjs esbuild.config.mjs vitest.config.mts package.json tsconfig.json biome.json",
    "lint:fix": "biome lint app src tests scripts build.mjs esbuild.config.mjs vitest.config.mts package.json tsconfig.json biome.json --write",
    "format": "biome format --write app src tests scripts build.mjs esbuild.config.mjs vitest.config.mts package.json tsconfig.json biome.json",
    "verify": "npm run lint && npm run typecheck && npm run test && npm run build && npm run docs:build",
    "release": "npm run build && npm run sbom && npm run scan && npm run licenses && npm run release:assets",
    "release:assets": "node scripts/create-release-assets.mjs",
    "sbom": "node scripts/create-prod-sbom.mjs",
    "scan": "mkdir -p security && grype sbom:security/sbom.spdx.json -o json > security/grype-report.json",
    "licenses": "mkdir -p security && grant check security/sbom.spdx.json --config .grant.yaml > security/grant-report.txt || { cat security/grant-report.txt; exit 1; }",
    "package": "npm pack",
    "prepublishOnly": "npm run verify",
    "precommit": "npm run lint && npm run typecheck && npm run test",
    "prepare": "husky",
    "docs:install": "npm --prefix docs ci"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@types/node": "25",
    "@vitest/coverage-v8": "4",
    "concurrently": "latest",
    "esbuild": "latest",
    "html-minifier-terser": "7",
    "http-server": "14",
    "husky": "9.1.7",
    "lightningcss": "1",
    "tsx": "4",
    "typescript": "6",
    "vitest": "4"
  },
  "dependencies": {
    "mikroserve": "1"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "workspaces": [
    "docs"
  ]
}